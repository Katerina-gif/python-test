## 📋 Тестирование Python-кода с использованием pytest

Этот проект содержит практические задания по тестированию Python-кода с использованием фреймворка `pytest`. Он демонстрирует основные концепции тестирования от простых ассертов до сложных фикстур и параметризации.

## 📁 Структура проекта

```
pytest_project/
├── functions.py          # Тестируемый код (функции и классы)
├── test_functions.py     # Тесты для кода
├── pytest.ini           # Конфигурация pytest (опционально)
└── README.md            # Этот файл
```

## 🔧 Установка и настройка

### Требования
- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Установка зависимостей
```bash
pip install pytest
```

### Запуск тестов
```bash
# Все тесты
pytest

# С подробным выводом
pytest -v

# Конкретный тестовый файл
pytest test_functions.py

# Тесты с определенным маркером
pytest -m "math"
```

## 📚 Описание задач

### Задача 1: Простая валидация пароля
**Функция**: `is_valid_password(password: str) -> bool`
- Проверяет минимальную длину пароля (8 символов)
- Возвращает `True` если длина ≥ 8 символов, иначе `False`

**Тестируемые сценарии**:
- Пароль корректной длины (10 символов)
- Пароль слишком короткий (5 символов)
- Пограничный случай (ровно 8 символов)

**Пример использования**:
```python
assert is_valid_password("password123") == True
assert is_valid_password("123") == False
assert is_valid_password("12345678") == True
```

### Задача 2: Определение возрастной группы
**Функция**: `get_age_group(age: int) -> str`
- Классифицирует возраст по категориям:
  - `"ребенок"` — до 13 лет
  - `"подросток"` — от 13 до 18 лет
  - `"взрослый"` — старше 18 лет

**Особенность тестирования**:
- Использование параметризации для множества сценариев
- Обязательная проверка пограничных значений

**Тестовые сценарии**:
```python
(12, "ребенок")
(13, "подросток")
(17, "подросток")
(18, "взрослый")
```

### Задача 3: Расчет стоимости корзины
**Функция**: `calculate_cart_total(cart_items: list[dict]) -> float`
- Рассчитывает общую стоимость товаров в корзине
- Каждый товар представлен словарем с ключами:
  - `name` — название товара
  - `price` — цена за единицу
  - `quantity` — количество

**Особенность тестирования**:
- Использование фикстур для подготовки тестовых данных
- Многократное использование одних и тех же данных в разных тестах

**Фикстура**:
```python
@pytest.fixture
def sample_cart():
    return [
        {"name": "apple", "price": 1.5, "quantity": 2},
        {"name": "banana", "price": 2.0, "quantity": 3},
        {"name": "orange", "price": 0.8, "quantity": 5},
    ]
```

**Пример расчета**:
```
apple: 1.5 * 2 = 3.0
banana: 2.0 * 3 = 6.0
orange: 0.8 * 5 = 4.0
Итого: 13.0
```

### Задача 4: Доступ к элементу словаря
**Функция**: `get_value_from_dict(data_dict: dict, key: str)`
- Возвращает значение по ключу из словаря
- При отсутствии ключа вызывает стандартное исключение `KeyError`

**Особенность тестирования**:
- Тестирование успешного сценария
- Тестирование исключений с использованием `pytest.raises`

**Пример теста**:
```python
# Успешный сценарий
assert get_value_from_dict({"a": 1}, "a") == 1

# Сценарий с ошибкой
with pytest.raises(KeyError):
    get_value_from_dict({"a": 1}, "b")
```

### Задача 5: Управление кошельком
**Класс**: `Wallet` с пользовательским исключением `InsufficientFundsError`
- **Методы**:
  - `__init__` — инициализация с начальным балансом
  - `deposit` — пополнение счета
  - `withdraw` — снятие средств

**Валидация**:
- Начальный баланс не может быть отрицательным
- Суммы пополнения и снятия должны быть положительными
- Нельзя снять больше средств, чем есть на счете

**Особенности тестирования**:
- Использование фикстур для создания экземпляров
- Тестирование успешных операций
- Тестирование исключений для ошибочных сценариев
- Параметризация для тестирования невалидных входных данных

**Пользовательское исключение**:
```python
class InsufficientFundsError(Exception):
    """Исключение, вызываемое при нехватке средств."""
    pass
```

**Примеры тестов**:
```python
# Успешное пополнение
wallet = Wallet(20.0)
wallet.deposit(10.0)
assert wallet.balance == 30.0

# Попытка снять больше, чем есть
with pytest.raises(InsufficientFundsError):
    wallet.withdraw(100.0)

# Невалидная сумма снятия
with pytest.raises(ValueError):
    wallet.withdraw(-5.0)
```

## 🧪 Ключевые концепции pytest

### 1. **Ассерты (Assertions)**
```python
assert условие, "сообщение об ошибке"
```

### 2. **Фикстуры (Fixtures)**
```python
@pytest.fixture
def подготовка_данных():
    return данные

def тест(подготовка_данных):
    assert функция(подготовка_данных) == ожидаемый_результат
```

### 3. **Параметризация (Parametrization)**
```python
@pytest.mark.parametrize("вход1, вход2, ожидаемый", [
    (1, 2, 3),
    (-1, 1, 0),
])
def тест(вход1, вход2, ожидаемый):
    assert функция(вход1, вход2) == ожидаемый
```

### 4. **Тестирование исключений**
```python
with pytest.raises(Исключение):
    функция_вызывающая_исключение()
```

### 5. **Маркеры (Markers)**
```python
@pytest.mark.math
def тест_математики():
    pass

@pytest.mark.user_profile
def тест_профиля():
    pass
```

## 📊 Пример вывода тестов

```
$ pytest -v
================================ test session starts =================================
collected 11 items

test_functions.py::test_password_valid PASSED [  9%]
test_functions.py::test_password_too_short PASSED [ 18%]
test_functions.py::test_password_boundary PASSED [ 27%]
test_functions.py::test_get_age_group[10-ребенок] PASSED [ 36%]
test_functions.py::test_get_age_group[12-ребенок] PASSED [ 45%]
test_functions.py::test_get_age_group[13-подросток] PASSED [ 54%]
test_functions.py::test_get_age_group[17-подросток] PASSED [ 63%]
test_functions.py::test_calculate_cart_total PASSED [ 72%]
test_functions.py::test_cart_contains_item PASSED [ 81%]
test_functions.py::test_get_value_success PASSED [ 90%]
test_functions.py::test_get_value_key_error PASSED [100%]

================================ 11 passed in 0.04s =================================
```

## 🔍 Отладка и устранение проблем

### Проблема: команда `python` не найдена
**Решение**:
```bash
# На macOS используйте python3
python3 --version
python3 -m pytest
```

### Проблема: вы находитесь в интерактивном режиме Python
**Признак**: видите приглашение `>>>`
**Решение**:
1. Нажмите `Ctrl + D` для выхода
2. Или напишите `exit()` и нажмите Enter
3. Перезапустите терминал

### Полезные флаги pytest
```bash
# Подробный вывод
pytest -v

# Остановиться после первого падения
pytest -x

# Запустить только упавшие тесты
pytest --lf

# Показать вывод print()
pytest -s

# Запустить тесты с определенным маркером
pytest -m "math"

# Исключить тесты с определенным маркером
pytest -m "not slow"
```

## 🚀 Дальнейшее развитие

### 1. **Добавление покрытия кода**
```bash
pip install pytest-cov
pytest --cov=functions --cov-report=html
```

### 2. **Конфигурационный файл `pytest.ini`**
```ini
[pytest]
markers =
    math: математические тесты
    user_profile: тесты профиля пользователя
    slow: медленные тесты

addopts = -v --strict-markers
```

### 3. **Интеграция с CI/CD**
Пример `.github/workflows/tests.yml`:
```yaml
name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - run: pip install pytest
      - run: pytest


